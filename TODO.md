<!-- fullWidth: false tocVisible: false tableWrap: true -->
# Identity

- more features that do what the name suggests: personalisation and tracking stuff

# Features

- have a setting to change themes
  - a theme changes only colors for now, no actual structural changes
  - can we make it so the app icon changes too depedning on the setting?

# Changes

- change exercise in routines to just have set count instead of defining every sets rep count and weight
  - those should be filled from history or be empty if no history is found
-  limit inputs in active sets to numbers (positive and negative)
- Improve charts
- restrict spec driven development
  - there are to many mismatches between specification and actual implementation
  - No idea how do ensure less drift